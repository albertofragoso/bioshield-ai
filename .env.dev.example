# Dev environment variables for docker-compose.yml
# Copy to .env.dev: cp .env.dev.example .env.dev
# .env.dev is gitignored — defaults below are safe for local dev only

POSTGRES_USER=bioshield
POSTGRES_PASSWORD=bioshield
