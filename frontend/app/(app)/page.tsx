// Dashboard — placeholder hasta que se diseñe la pantalla (Prompt 3 / Fase C)
export default function DashboardPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[calc(100vh-56px)] gap-4 p-8">
      <p className="font-mono text-[11px] text-subtext uppercase tracking-[0.1em]">
        Dashboard — en construcción
      </p>
      <p className="font-sans text-sm text-subtext/60">
        Prompt 3 pendiente de diseño en claude.ai/design
      </p>
    </div>
  );
}
