# BioShield AI — Product Pulse
**Window:** 2026-06-05 15:04 → 2026-06-06 15:04 (24h, -15min buffer)
**Source:** DB read-only (bioshield.db) · No analytics/tracing (pending instrumentation)
**Node timing logs:** Not available — feat/langgraph-node-timing pending merge; backend not active in window

---

## Headlines

- **0 activity in the 24h window** — dev environment, no active sessions during this period. All data below reflects cumulative dev state.
- **17.4% error rate** across all-time scans (15/86) — all errors correlate with PENDING semaphore, indicating pipeline failures before completion.
- **confidence_score = null in all recent scans** — field not being populated by the current pipeline; 58.3% average comes from older records only.

---

## Usage

| Metric | 24h window | All-time (dev) |
|---|---|---|
| New users (`user_registered`) | 0 | 6 |
| Scans started (`scan_started`) | 0 | 86 |
| Scans completed (`scan_completed`) | 0 | 71 |
| Scan error rate | — | 17.4% (15/86) |
| Biomarker uploads (`biosync_completed`) | 0 | 1 user active |
| `alternatives_viewed` | no data (pending) | no data (pending) |

**Scan semaphore distribution (all-time):**

| Result | Count | % |
|---|---|---|
| YELLOW | 30 | 34.9% |
| ORANGE | 19 | 22.1% |
| GRAY | 17 | 19.8% |
| PENDING (error) | 15 | 17.4% |
| RED | 4 | 4.7% |
| BLUE | 1 | 1.2% |

61.6% of completed scans (53/86) detected at least one conflict with `conflict_severity` populated.

**Biosync adoption:** 1 of 6 dev users has uploaded biomarkers (16.7%). The 3 most recent scans from that user show ORANGE + MEDIUM/HIGH conflict severity, consistent with personalized detection firing. The other 7/10 recent scans (no biomarkers) show GRAY or YELLOW — generic analysis path.

---

## Quality Scoring — personalization_relevance

**Method:** Structural signal from DB; result_json not sampled (biomarker data encrypted).

Sample: 10 most recent scans (2026-05-30 to 2026-05-31)

| Scans with active biomarkers | Scans without biomarkers |
|---|---|
| 3 | 7 |

- Scans **with** biomarkers: all 3 resulted in ORANGE + conflict_severity (MEDIUM or HIGH) — personalization path appears to fire correctly.
- Scans **without** biomarkers: mix of GRAY/YELLOW with no conflicts — expected generic path.

**Personalization_relevance score: cannot compute** — result_json inspection required to confirm insights reference biomarker data vs generic output. Structural signal is promising but not conclusive.

**Action needed:** To enable quality scoring, add a non-PHI field to `result_json` (e.g., `personalized: bool`, `biomarker_signals_used: int`) that indicates whether biosync influenced the output. This allows quality sampling without decrypting biomarker data.

---

## System Performance

**Node timing:** No data. `timed_node` instrumentation exists on `feat/langgraph-node-timing` (pending merge). After merge, run ≥20 scans to establish P95 baseline before calibrating `SLOW_NODE_THRESHOLDS`.

**DB health (dev):**
- Products catalog: 16,042 entries (healthy seed)
- Average confidence_score: 0.583 — but **null in all 10 most recent scans**. This suggests the field stopped being written at some point. Last non-null value predates current pipeline version.

**Errors:** 15 scans with `status=error` and `semaphore_result=PENDING` — pipeline died mid-run. Without tracing, root cause unknown. Error rate of 17.4% in dev warrants investigation before any soft launch.

---

## Followups

1. **Investigate 17.4% error rate.** 15 scans died with PENDING semaphore. Add error logging to the scan router endpoint to capture which node fails and why. Candidate: LangGraph exception swallowed before status update.

2. **Fix confidence_score not being written.** All recent scans have `null`. Identify which node/accumulator is responsible for writing this field and confirm it's still wired. May have regressed during the `ScanStateAccumulator.apply()` refactor.

3. **Add `personalized: bool` to result_json.** Required to enable quality scoring without PHI exposure. Without it, `personalization_relevance` cannot be measured programmatically.

4. **Merge feat/langgraph-node-timing.** After merge, run ≥20 representative scans and update `SLOW_NODE_THRESHOLDS` to P95+20% per the module docstring requirement before considering production-ready.

5. **Instrument analytics.** All pulse events (`scan_started`, `scan_completed`, `user_registered`, etc.) currently show "no data". Without event instrumentation, future pulses will continue to rely on DB counts alone — no funnel, no drop-off signal, no session context.

---

*Generated by ce-product-pulse · BioShield AI · 2026-06-06 15:19 · Sources: bioshield.db (read-only)*
