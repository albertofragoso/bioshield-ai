# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv*/
.mypy_cache/

# Test coverage
.coverage*
htmlcov/

# Environment
.env
.env.dev
.env.prod
.tunnel_url

# Production data (never commit)
data/

# Databases
*.db
*.sqlite3
!tests/fixtures/chroma-seed/chroma.sqlite3
*.db-journal
*.db-wal

# Vector store
chroma_db/
bioshield_chroma_*_backup_*.tar.gz

# Frontend
node_modules/
.next/

# OS
.DS_Store

# Claude Code (local settings)
.claude/CLAUDE.local.md
.claude/settings.local.json
.vscode/

# Golden dataset — generated files (keep .gitkeep, ignore JSON outputs)
backend/data/golden/*.json

# OFF ingestion output (large generated data, not committed)
backend/scripts/data/

# Misc
*.pdf
!tests/fixtures/biosync-test.pdf
cookies.txt

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
tests/fixtures/files/oversized.pdf
tests/fixtures/integration-auth.json
.superpowers/

# Ingestion script output (can be large, regenerated on demand)
backend/scripts/data/
.worktrees/

# Compound Engineering — machine-local config (never commit)
.compound-engineering/config.local.yaml

# Video assets — large files, serve from CDN in prod; copy manually in dev
frontend/public/videos/*.mp4
frontend/public/videos/*.webm
